kind: "Template"
apiVersion: "v1"
metadata:
  name: "deploy-template"
  annotations:
    description: "Template for deployment"
objects:
  - kind: Service
    apiVersion: v1
    metadata:
      name: svc-optisam-simulation
      annotations:
        description: Exposes and load balances the application pods
      labels:
        run: optisam-simulation
    spec:
      ports:
        - name: api
          port: 80
          targetPort: 8080
          protocol: TCP
        - name: instapi
          port: 81
          targetPort: 8081
          protocol: TCP
        - name: grpc
          port: 8090
          targetPort: 8090
          protocol: TCP                
      selector:
        app: optisam-simulation
  - kind: Route
    apiVersion: v1
    metadata:
      name: route-svc-optisam-simulation
      labels:
        run: optisam-simulation
    spec:
      host: optisam-simulation-${ENV}.apps.fr01.paas.tech.orange
      to:
        kind: Service
        name: svc-optisam-simulation
      port:
        targetPort: api        
      tls:
        termination: edge
  - kind: Route
    apiVersion: v1
    metadata:
      name: route-svc-optisam-simulation-instrumentation
      labels:
        run: optisam-simulation
    spec:
      host: optisam-simulation-inst-${ENV}.apps.fr01.paas.tech.orange
      to:
        kind: Service
        name: svc-optisam-simulation
      port:
        targetPort: instapi        
      tls:
        termination: edge
  - kind: DeploymentConfig
    apiVersion: v1
    metadata:
      name: optisam-simulation
      labels:
        app: optisam-simulation
    spec:
      replicas: ${{REPLICAS}}
      selector:
        app: optisam-simulation
      strategy:
        type: Rolling
      template:
        metadata:
          annotations:
            prometheus.io/port: '8081'
            prometheus.io/scrape: 'true'
          labels:
            app: optisam-simulation
            internal-elasticsearch: 'true'  
          name: optisam-simulation
        spec:
          containers:
            - name: optisam-simulation
              image: ${DOCKER_REGISTRY}/optisam/simulation-service:${TAG}
              imagePullPolicy: Always
              env:
                - name: "ENV"
                  value: ${ENV}
                - name: "DB_PASSWORD"  
                  value: ${DB_PASSWORD}
              ports:
                - containerPort: 8080
                  protocol: TCP
                - containerPort: 8081
                  protocol: TCP
                - containerPort: 8090
                  protocol: TCP
              livenessProbe:
                failureThreshold: 3
                httpGet:
                  path: /healthz
                  port: 8081
                  scheme: HTTP
                initialDelaySeconds: 15
                periodSeconds: 10
                successThreshold: 1
                timeoutSeconds: 2
              readinessProbe:
                failureThreshold: 3
                httpGet:
                  path: /healthz
                  port: 8081
                  scheme: HTTP
                initialDelaySeconds: 15
                periodSeconds: 10
                successThreshold: 1
                timeoutSeconds: 2
              resources:
                requests:
                  cpu: "80m"
                  memory: "128Mi"
                limits:
                  cpu: "100m"
                  memory: "150Mi"
              volumeMounts:
                - name: config-volume
                  mountPath: /opt/config
                - name: secret-volume
                  mountPath: /opt/secret
                  readOnly: true
          volumes:
            - name: config-volume
              configMap:
                name: config-simulation-${ENV}
            - name: secret-volume
              secret:
                secretName: secret-apipublickey-${ENV}
parameters:
  - name: DB_PASSWORD
    description: Postgres Database Password
    required: true
  - name: TAG
    description: Tag for Docker image
    value: "latest"
    required: true
  - name: ENV
    description: Env for deployment
    required: true
  - name: REPLICAS
    description: Pod replica count
    value: "1"
    required: true
  - name: DOCKER_REGISTRY
    description: Artifactory URL to download images
    value: "optisam-it-virt-docker-unstable.repos.tech.orange"
    required: true
