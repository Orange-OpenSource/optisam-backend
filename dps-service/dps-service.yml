kind: "Template"
apiVersion: "v1"
metadata:
  name: "deploy-template"
  annotations:
    description: "Template for deployment"
objects:
  - kind: Service
    apiVersion: v1
    metadata:
      name: svc-optisam-dps-${ENV}
      annotations:
        description: Exposes and load balances the dps pods
      labels:
        run: optisam-dps-${ENV}
    spec:
      ports:
        - name: api
          port: 80
          targetPort: 8080
          protocol: TCP
        - name: instapi
          port: 81
          targetPort: 8081
          protocol: TCP          
        - name: grpc
          port: 8090
          targetPort: 8090
          protocol: TCP
      selector:
        app: optisam-dps-${ENV}
  - kind: Route
    apiVersion: v1
    metadata:
      annotations:
        haproxy.router.openshift.io/timeout: 300s
      name: route-svc-optisam-dps-${ENV}
      labels:
        run: optisam-dps-${ENV}
    spec:
      host: optisam-dps-${ENV}.apps.fr01.paas.tech.orange
      to:
        kind: Service
        name: svc-optisam-dps-${ENV}
      port:
        targetPort: api
      tls:
        termination: edge
  - kind: Route
    apiVersion: v1
    metadata:
      name: route-svc-optisam-dps-instrumentation-${ENV}
      labels:
        run: optisam-dps-${ENV}
    spec:
      host: optisam-dps-inst-${ENV}.apps.fr01.paas.tech.orange
      to:
        kind: Service
        name: svc-optisam-dps-${ENV}
      port:
        targetPort: instapi        
      tls:
        termination: edge        
  - kind: DeploymentConfig
    apiVersion: v1
    metadata:
      name: optisam-dps-${ENV}
      labels:
        app: optisam-dps-${ENV}
    spec:
      replicas: ${{REPLICAS}}
      selector:
        app: optisam-dps-${ENV}
      strategy:
        type: Rolling
      template:
        metadata:
          annotations:
            prometheus.io/port: '8081'
            prometheus.io/scrape: 'true'
          labels:
            app: optisam-dps-${ENV}
            internal-elasticsearch: 'true'  
          name: optisam-dps-${ENV}
        spec:
          containers:
            - name: optisam-dps-${ENV}
              image: ${DOCKER_REGISTRY}/optisam/dps-service-${ENV}:${TAG}
              imagePullPolicy: Always
              env:
                - name: "ENV"
                  value: ${ENV}
                - name: "DB_PASSWORD"  
                  value: ${DB_PASSWORD}
              ports:
                - containerPort: 8080
                  protocol: TCP
                - containerPort: 8090
                  protocol: TCP
                - containerPort: 8081
                  protocol: TCP
              envFrom:
                - secretRef:
                       name: user-access
              livenessProbe:
                failureThreshold: 3
                httpGet:
                  path: /healthz
                  port: 8081
                  scheme: HTTP
                initialDelaySeconds: 15
                periodSeconds: 10
                successThreshold: 1
                timeoutSeconds: 2
              readinessProbe:
                failureThreshold: 3
                httpGet:
                  path: /healthz
                  port: 8081
                  scheme: HTTP
                initialDelaySeconds: 15
                periodSeconds: 10
                successThreshold: 1
                timeoutSeconds: 2
              resources:
                requests:
                  cpu: "1200m"
                  memory: "1900Mi"
                limits:
                  cpu: "3100m"
                  memory: "7900Mi"
              volumeMounts:
                - name: config-volume-${ENV}
                  mountPath: /opt/config/
                - name: optisam-dir
                  mountPath: /optisam_dir
                - name: secret-volume
                  mountPath: /opt/secret
                  readOnly: true
          volumes:
            - name: config-volume-${ENV}
              configMap:
                name: config-dps-${ENV}
            - name: optisam-dir
              persistentVolumeClaim:
                claimName: optisam-storage
            - name: secret-volume
              secret:
                secretName: secret-apipublickey-${ENV}
parameters:
  - name: DB_PASSWORD
    description: Postgres Database Password
    required: true
  - name: TAG
    description: Tag for Docker image
    value: "latest"
    required: true
  - name: ENV
    description: Env for deployment
    required: true
  - name: REPLICAS
    description: Pod replica count
    value: "1"
    required: true
  - name: DOCKER_REGISTRY
    description: Artifactory URL to download images
    value: "optisam-it-virt-docker-unstable.repos.tech.orange"
    required: true
 
