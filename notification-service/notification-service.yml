---
kind: "Template"
apiVersion: "v1"
metadata:
  name: "deploy-template"
  annotations:
    description: "Template for deployment"
objects:
  - kind: Service
    apiVersion: v1
    metadata:
      name: svc-optisam-notification-${ENV}
      annotations:
        description: Exposes and load balances the application pods
      labels:
        run: optisam-notification-${ENV}
    spec:
      ports:
        - name: api
          port: 80
          targetPort: 8080
          protocol: TCP
        - name: instapi
          port: 81
          targetPort: 8081
          protocol: TCP
        - name: grpc
          port: 8090
          targetPort: 8090
          protocol: TCP
      selector:
        app: optisam-notification-${ENV}
  - kind: Route
    apiVersion: v1
    metadata:
      name: route-svc-optisam-notification-${ENV}
      labels:
        run: optisam-notification-${ENV}
    spec:
      host: optisam-notification-${ENV}.apps.fr01.paas.tech.orange
      to:
        kind: Service
        name: svc-optisam-notification-${ENV}
      port:
        targetPort: api
      tls:
        termination: edge
  - kind: Route
    apiVersion: v1
    metadata:
      name: route-svc-optisam-notification-instrumentation-${ENV}
      labels:
        run: optisam-notification-${ENV}
    spec:
      host: optisam-notification-inst-${ENV}.apps.fr01.paas.tech.orange
      to:
        kind: Service
        name: svc-optisam-notification-${ENV}
      port:
        targetPort: instapi
      tls:
        termination: edge
  - kind: DeploymentConfig
    apiVersion: v1
    metadata:
      name: optisam-notification-${ENV}
      labels:
        app: optisam-notification-${ENV}
    spec:
      replicas: ${{REPLICAS}}
      selector:
        app: optisam-notification-${ENV}
      strategy:
        type: Rolling
      template:
        metadata:
          annotations:
            prometheus.io/port: '8081'
            prometheus.io/scrape: 'true'
          labels:
            app: optisam-notification-${ENV}
            internal-elasticsearch: 'true'
          name: optisam-notification-${ENV}
        spec:
          containers:
            - name: optisam-notification-${ENV}
              image: ${DOCKER_REGISTRY}/optisam/notification-service-${ENV}:${TAG}
              imagePullPolicy: Always
              env:
                - name: "ENV"
                  value: ${ENV}
                - name: "MIG_DIR"
                  value: ${MIG_DIR}
                - name: "MIG_VERSION"
                  value: ${MIG_VERSION}
              envFrom:
                - secretRef:
                    name: notification
                - secretRef:
                    name: user-access
                - secretRef:
                    name: db-access
                - secretRef:
                    name: db-pass
              ports:
                - containerPort: 8080
                  protocol: TCP
                - containerPort: 8090
                  protocol: TCP
                - containerPort: 8081
                  protocol: TCP
              livenessProbe:
                failureThreshold: 3
                httpGet:
                  path: /healthz
                  port: 8081
                  scheme: HTTP
                initialDelaySeconds: 15
                periodSeconds: 10
                successThreshold: 1
                timeoutSeconds: 2
              readinessProbe:
                failureThreshold: 3
                httpGet:
                  path: /healthz
                  port: 8081
                  scheme: HTTP
                initialDelaySeconds: 15
                periodSeconds: 10
                successThreshold: 1
                timeoutSeconds: 2
              resources:
                requests:
                  cpu: "1m"
                  memory: "5Mi"
                limits:
                  cpu: "40m"
                  memory: "300Mi"
              volumeMounts:
                - name: config-volume-${ENV}
                  mountPath: /opt/config
                - name: secret-volume
                  mountPath: /opt/secret
                  readOnly: true
                - name: secret-volume-ca
                  readOnly: true
                  mountPath: /opt/secret-ca
                - name: secret-volume-client
                  readOnly: true
                  mountPath: /opt/secret-client
                - name: secret-volume-key
                  readOnly: true
                  mountPath: /opt/secret-key

          volumes:
            - name: config-volume-${ENV}
              configMap:
                name: config-notification-${ENV}
            - name: secret-volume
              secret:
                secretName: secret-apipublickey-${ENV}
            - name: secret-volume-ca
              secret:
                secretName: kafka-ca.crt-${ENV}
            - name: secret-volume-client
              secret:
                secretName: kafka-client.crt-${ENV}
            - name: secret-volume-key
              secret:
                secretName: kafka-client.key-${ENV}
parameters:
  - name: MIG_DIR
    description: Postgres Database variable
    required: true
  - name: MIG_VERSION
    description: Postgres Database variable
    required: true
  - name: TAG
    description: Tag for Docker image
    value: "latest"
    required: true
  - name: ENV
    description: Environment for deployment
    required: true
  - name: REPLICAS
    description: Pod replica count
    value: "1"
    required: true
  - name: DOCKER_REGISTRY
    description: Artifactory URL to download images
    value: "optisam-it-virt-docker-unstable.repos.tech.orange"
    required: true
