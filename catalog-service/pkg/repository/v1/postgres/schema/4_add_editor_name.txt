-- +migrate Up
-- SQL in section 'Up' is executed when this migration is applied
Alter Table product  ADD COLUMN editor_name VARCHAR NOT NULL;

-- +migrate Down
-- SQL section 'Down' is executed when this migration is rolled back
ALTER TABLE product DROP COLUMN editor_name;
